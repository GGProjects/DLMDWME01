---
title: "Data Dictionary: Standby Data (.csv)"
author: "Georg Grunsky"
date: today
editor: visual
format:
  pdf:
    latex_engine: xelatex
    keep-tex: true
    papersize: a4
    documentclass: subfiles


---


```{r}
#| include: false
require(readr)

filename <- "sickness_table.csv"
path <- "../../01_data/01_lake/01_use_case_2/"
sickness <- read_csv(paste0(path, filename), 
        col_types = cols(...1 = col_double(), 
        date = col_date(format = "%Y-%m-%d")))
```

## Data description

File name: **`{r} filename`**

Saved in: **`{r} path`**

The dataset has **`{r} nrow(sickness)`** observations of **`{r} length(names(sickness))`** variables and is of type **`{r} class(sickness)`**.

The data structure shows following data types:

```{r}
#| echo: false
attributes(sickness)$spec
```

The description of the dataset is taken from the projects basic data document "Zusatzinformationen_DLMDWME01

The file provides data about: Anzahl an Krankenständen, Notrufen, Ersatzfahrern im Bereitschaftsdienst und Zahl der zusätzlichen Einsatzfahrer, die aktiviert werden müssen (auf Tagesbasis)

Description of variables

-   ...1: Zähler
-   date: Datum
-   n_sick: Anzahl der Einsatzfahrer, die einen Krankenstand angemeldet haben
-   calls: Anzahl der Notrufe
-   n_duty: Anzahl der Einsatzfahrer im Dienst
-   n_sby: Anzahl der verfügbaren Ersatzfahrer
-   sby_need: Anzahl der Ersatzfahrer, die aktiviert werden
-   dafted: Anzahl der zusätzlichen Einsatzfahrer, die aktiviert werden müssen, wenn die Anzahl der Einsatzfahrer im Bereitschaftsdienst nicht ausreicht

```{r}
#| eval: false
#| include: false

source_file <- "dict_csv"
target <- "dlmdwme01_uc2_csv.pdf"
target_dir <- paste0(wd, "/00_docs/03_artifacts/02_data_reports/00_dictionaries/")
paper_target <- paste0(wd, "/00_docs/03_artifacts/01_paper/041-Annexes.tex")
rel_paper_target <- "../02_data_reports/00_dictionaries/"
quarto::quarto_render(input = paste0(source_file, ".qmd"),
                      output_file = target)
system(paste0("mv ",target, " ", target_dir))

system(paste0("mv ",source_file, ".tex ", target_dir))

readr::write_lines(paste0("\\input{", rel_paper_target, source_file, "}"), 
            file = paper_target, 
            append = TRUE)
```

